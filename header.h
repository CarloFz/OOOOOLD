#pragma once
#include <stdio.h>
#include <iostream>
#include <string.h>
#include <string>
using namespace std;

int inputProcess(int argc, char** argv);
int create(int count);
int solve(char *path);